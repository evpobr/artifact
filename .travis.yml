dist: trusty
group: edge
language: c
matrix:
  exclude:
    - os: osx
      compiler: clang

script:
  - mkdir build
  - cd build
  - cmake .. -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=ON -DCMAKE_C_FLAGS="-Wall -Wextra"
  - cmake --build .
